import styled from 'styled-components';

const Container = styled.div`
  margin-top: 2%;
`;

interface Props {}

export function RokuRemote({}: Props): JSX.Element {
  return <Container>ROKU</Container>;
}
